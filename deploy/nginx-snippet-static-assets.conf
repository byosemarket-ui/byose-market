# Serve static storefront assets directly — never SPA-fallback to index.html for missing JS/CSS.
location ~* \.(?:js|mjs|css|map|json|woff2?|ttf|eot|svg)$ {
    try_files $uri =404;
    expires 7d;
    add_header Cache-Control "public, max-age=604800" always;
}
